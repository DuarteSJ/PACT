--- a/perf.c	2026-05-14 13:00:55.041646574 -0500
+++ b/perf.c	2026-05-14 13:05:10.033133249 -0500
@@ -38,6 +38,7 @@
 #include <linux/kernel.h>
 #include <linux/string.h>
 #include <linux/zalloc.h>
+#include "util/evsel.h"
 
 const char perf_usage_string[] =
 	"perf [--version] [--help] [OPTIONS] COMMAND [ARGS]";
@@ -462,6 +463,16 @@
 	const char *cmd;
 	char sbuf[STRERR_BUFSIZE];
 
+	int pac_shmid;
+	if ((pac_shmid = shmget(PAC_SHM_KEY, PAC_SHM_BUFSZ, IPC_CREAT | 0666)) < 0) {
+		perror("shmget");
+		exit(1);
+	}
+	if ((pac_shm = (char **)shmat(pac_shmid, NULL, 0)) == (char **)-1) {
+		perror("shmat");
+		exit(1);
+	}
+
 	perf_debug_setup();
 
 	/* libsubcmd init */
--- a/util/evsel.h	2026-05-14 13:00:55.041646574 -0500
+++ b/util/evsel.h	2026-05-14 13:05:27.181367709 -0500
@@ -11,6 +11,15 @@
 #include <perf/evsel.h>
 #include "symbol_conf.h"
 
+/* PAC shared-memory interface — initialised in main(), written in evsel__compute_deltas() */
+#include <sys/ipc.h>
+#include <sys/shm.h>
+
+#define PAC_SHM_KEY   5679
+#define PAC_SHM_BUFSZ 20
+
+extern char **pac_shm;
+
 struct bpf_object;
 struct cgroup;
 struct perf_counts;
--- a/util/evsel.c	2026-05-14 13:00:55.041646574 -0500
+++ b/util/evsel.c	2026-05-14 13:05:23.757320894 -0500
@@ -55,6 +55,15 @@
 #include <internal/threadmap.h>
 
 #include <linux/ctype.h>
+#include <stdio.h>
+
+/* PAC shared-memory — definition; declared extern in evsel.h */
+char **pac_shm = NULL;
+
+/* per-interval event counters for evsel__compute_deltas */
+static int           event_idx     = 0;
+static unsigned long stall_cycles  = 0;
+static unsigned long remote_misses = 0;
 
 #ifdef HAVE_LIBTRACEEVENT
 #include <traceevent/event-parse.h>
@@ -1532,6 +1541,38 @@
 	*perf_counts(evsel->prev_raw_counts, cpu_map_idx, thread) = *count;
 
 	count->val = count->val - tmp.val;
+
+	switch (event_idx) {
+	case 0:
+		event_idx++;
+		break;
+	case 1:
+		stall_cycles = count->val;
+		event_idx++;
+		break;
+	case 2: {
+		double ratio;
+		char   buf[PAC_SHM_BUFSZ];
+
+		remote_misses  = count->val;
+		ratio = (remote_misses > 0)
+			? (double)stall_cycles / (double)remote_misses
+			: 0.0;
+		if (ratio > 999999) {
+			ratio = 0.0;
+			fprintf(stderr, "pac: stall ratio overflow, reset\n");
+		}
+		snprintf(buf, sizeof(buf), "%2.6f", ratio);
+		memcpy(pac_shm, buf, sizeof(buf));
+
+		event_idx    = 0;
+		stall_cycles = 0;
+		break;
+	}
+	default:
+		break;
+	}
+
 	count->ena = count->ena - tmp.ena;
 	count->run = count->run - tmp.run;
 }
